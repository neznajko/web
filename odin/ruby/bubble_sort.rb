################################################################
################################################################
################################################################
################################################################
# Assignment
# Build a method #bubble_sort that takes an array and returns a sorted
# array. It must use the bubble sort methodology (using #sort would
# be pretty pointless, wouldn’t it?).
# > bubble_sort([4,3,78,2,0,2])
# => [0,2,2,3,4,78]
################################################################
################################################################
################################################################
################################################################
def suap( a, i, j )
  t = a[i]
  a[i] = a[j]
  a[j] = t
end
################################################################
def bubble_sort( a )
  n = a.length
  while n > 0
    p a
    done = true
    (1...n).each do |j|
      if a[j] < a[j - 1]
        suap( a, j - 1, j )
        done = false
      end
    end
    break if done
    n -= 1
  end
end
###############################################################=
a = [4,3,78,2,0,2]
bubble_sort( a )
################################################################
################################################################
################################################################
################################################################
